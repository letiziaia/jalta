import { memo } from 'react';
import useChartUpdate from '../UseChartUpdate';
var Caption = /*#__PURE__*/memo(function (props) {
  useChartUpdate(props, updateCaption, function (chart) {
    return updateCaption(chart, {
      text: null
    });
  });
  return null;
});

var updateCaption = function updateCaption(chart, config) {
  chart.setCaption(config);
};

Caption.displayName = 'Caption';
export default Caption;